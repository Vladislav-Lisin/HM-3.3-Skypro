select * from student
WHERE age > 10 AND age < 20;

SELECT name from student;

SELECT * FROM student
WHERE name LIKE '%a%';

SELECT * FROM student
WHERE age < id;

SELECT name
FROM student
ORDER BY name;



